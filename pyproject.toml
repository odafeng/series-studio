[project]
name = "series-studio"
version = "0.1.0"
description = "A config-driven, multi-agent production system for episodic video"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
